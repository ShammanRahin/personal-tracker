services:
  - type: web
    name: tracker-api
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: GROQ_API_KEY
        sync: false
      - key: GOOGLE_TOKEN_JSON
        sync: false
    healthCheckPath: /
